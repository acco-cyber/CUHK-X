# Declarations

- `CUHK-X_Honor_Declaration.docx`: must be completed and signed by **every** team member (koushikrudra, nabidnur).
  Add the signed copy here as `CUHK-X_Honor_Declaration_signed.pdf`, or as a signed DOCX, and update
  `manifest.yaml` (`declarations.signed_declaration`).
- `CLARIFICATION.md`: the team's written clarification, which forms part of the declaration. It covers the answers
  set by hand, the provenance of the code, public-leaderboard use and AI assistance. The declaration should reference
  it next to the statements it qualifies.
