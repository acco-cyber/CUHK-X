# Pure model output: identical stages and parameters, WITHOUT any disclosed adjustment.
# This is also exactly what final.yaml produces on any test set other than the original one.
{
  "name": "model only (no disclosed adjustments)",
  "model_state": "artifacts/model_state.json",
  "seed": null,
  "precision": "float64 (Python float)",
  "batch_size": null,
  "two_repeat_positions": [2, 3],
  "match_ratio_threshold": 0.85,
  "repair_ratio_threshold": 0.45,
  "harn_order_penalty": 12.0,
  "duration_weight": 1.0,
  "stages": {
    "slot_rule": true,
    "scene_joint": true,
    "singleton_emotion": true,
    "duration_emotion": true
  }
}
